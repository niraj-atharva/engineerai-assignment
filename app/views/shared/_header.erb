<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to 'Engineer.ai', root_path, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class="active"><a href="#">Tweets <span class="sr-only">(current)</span></a></li>
        <% if user_signed_in? %>
            <li><%= link_to 'Add Tweet', '#' %></li>
        <% end %>
        <li><a href="#">Users</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <p class="navbar-text navbar-right">Signed in as <b><%= current_user.name %></b></p>
            <li><%= link_to 'Logout', destroy_user_session_path, method: :delete, alert: 'Are you sure?' %></li>
        <% else %>
            <li><%= link_to 'SignIn/SignUp', new_user_session_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>